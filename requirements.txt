# ===== PU-CGCNN 项目依赖 =====
# Python 版本建议: 3.10 - 3.12
#
# 说明: 代码中在 RTX 5070 Ti (Blackwell, sm_120) 上训练，
# 需要 PyTorch >= 2.6 (对应 CUDA 12.8 wheel)，否则会报
# "no kernel image is available for execution on the device"。
# 安装命令（Windows/Linux, CUDA 12.8）:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# 然后再执行:
#   pip install -r requirements.txt

# ---- 深度学习框架 ----
torch>=2.6.0

# ---- 科学计算 / 数据处理 ----
numpy>=1.24,<2.3
pandas>=2.0
scikit-learn>=1.3

# ---- 晶体结构处理 (CGCNN 所需) ----
pymatgen>=2024.1.1

# ---- 进度条 ----
tqdm>=4.65

# ---- 可视化 ----
matplotlib>=3.7
seaborn>=0.12

# ---- Web UI (app.py 使用) ----
streamlit>=1.30
